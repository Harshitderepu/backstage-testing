pipeline {
    agent any

    stages {
        stage('checkout scm') {
            steps {
                sh 'checkout scm'
            }
        }
        stage('build') {
            steps {
                sh '
    }
}
